---
interface Props {
    text: string;
}

const text = Astro.props.text;
---

<code>{text}</code>

<style>
    code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
</style>